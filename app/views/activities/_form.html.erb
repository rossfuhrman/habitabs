<%= form_for(@activity) do |f| %>
  <% if @activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity.errors.count, "error") %> prohibited this habit from being saved:</h2>

      <ul>
      <% @activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <label>This is habit is: </label><br>
    <%= f.radio_button :polarity, Activity::NEUTRAL  %> 
    <label>Neutral</label>
    <%= f.radio_button :polarity, Activity::POSITIVE %>
    <label>+ Positive</label>
    <%= f.radio_button :polarity, Activity::NEGATIVE %>
    <label>- Negative</label>
  </div>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>

  <br />

<% end %>
